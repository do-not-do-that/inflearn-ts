export interface ICalculator {
  add(a: number, b: number): number;

  minus(a: number, b: number): number;

  multiple(a: number, b: number): number;

  divide(a: number, b: number): number;
}

// 인터페이스를 이용해서 명세와 설계 진행
